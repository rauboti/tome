import { PageHeader } from '@rauboti/ui'

/** Placeholder (T020): the campaign list + role-aware campaign view land with US2 (T044–T045). */
export const CampaignsPage = () => <PageHeader title="Campaigns" />
